  1. Класс с полями и перекрытым методом созданы. 

  2. Создал 3 объекта. Ниже произвден теоретический расчёт того, сколько памяти занимает каждый из них.

        user 1:
        Заголовок переменной user1: 16 байт для 64-разрядной системы
        Переменная int age: 4 , байта
        Итого, с учётом выравнивания для кратности 8: 24 байта

        Класс String содержит в себе следующие поля:
          private final char value[];
          private final int offset;
          private final int count;
          private int hash;
        Заголовок переменной name: 16 байт
        Поля int: 4 * 3 == 12 байт
        Ссылочная переменная на объект массива: 4 байта
        Итого String name = new String("Oleg"): 32 байта

        char value = new char[4]
        Заголовок: 16 байт + 4 байта на длину массива == 20 байт
        Примитивы char: 2 байта * 4 == 8 байт
        Итого, с учётом выравнивания: 32 байта

        Итого, user1: 88 байт.

        У user2 и user3 будет отличаться количество char в строке:
        user2: 2 байта * 9 = 18 байт
        user3: 2 байта * 5 = 10 байт

        Итого, user2: 96 байт
        Итого, user3: 88 байт

  3. Предполагаю, что если объект не имеет полей - объем занимаемой им памяти будет равен размеру заголовка,
  т. е. 8 и 16 байт для 32- и 64-разрядных систем соотвественно.
  4. Удалось добиться такого состояния с помощью ключей  -Xmx3m -Xms1m
  5. Начальный размер heap равен 1мб. Мы создаём множество объектов без ссылок. Когда память в eden заполняется, запускается minorGC.
   Объекты без ссылок удаляются. Объекты с ссылками(user1, user2 и user3) перемещаются в survior. Только возник вопрос:
   Если при minorGC удлаяются объекты без ссылок - почему удаляются не все объекты созданные в цикле, это происходит как-то выборочно?
    В моём случае, при запуске программы удалился объект с name32 а также name9940, name9941 и т.д...
        
